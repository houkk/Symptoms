# What do you need to prepare befor run the project?
```
$ The package in the requirement.txt is all that the project used.
$ Usually, the pip is enough, you can try the easy_install if the pip is unuseful.
```